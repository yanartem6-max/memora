{
  "name": "memora",
  "version": "1.0.0",
  "description": "MEMORA - Premium Telegram Mini App for Social Crypto Trading",
  "private": true,
  "engines": {
    "node": "18.17.0",
    "npm": "9.x"
  },
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev -w backend",
    "build": "npm run build -w backend",
    "start": "cd backend && npm start",
    "test": "npm test -w backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
